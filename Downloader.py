import os
import webbrowser
from sys import *
import readline
with urllib.request import urlopen

def is_dowloadable(url):
    h = request.head(url, allow_redirects = True)
    header = h.headers
    content

def MarvellousDownload(url):
    allowed = is_dowloadable

    if 
def main():

    print("----Siddhesh Kamble----")

    print("Application name :", argv[0])

    if(len(argv) != 2):
        print("Invalid number of arguments")
        exit()

    if((argv[1]=="-h") or (argv[1]=="-H")):
        print("The script is used to open Url's in file")
        exit()

    if((argv[1]=="-u") or (argv[1]=="-U")):
        print("Usage : ApplicationName FileName")
        exit()

    url = ''

    result = MarvellousDownload

    # try:
    #     connected = is_connencted()

    #     if connected:
    #         WebLauncher(argv[1])
    #     else:
    #         print("Unable to connect to internet...")

    # except ValueError:
    #     print("Error : Invalid datatype of input")

    # except Exception as E:
    #     print("Error : Invaild Inputs", E)


if __name__ == "__main__":
    main()